[build]
  command = "npm run build"
  publish = "frontend/dist"

[dev]
  command = "npm run dev --prefix frontend"
  port = 5173

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[functions]
  node_bundler = "esbuild"
